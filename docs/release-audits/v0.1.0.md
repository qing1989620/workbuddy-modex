# OMMW v0.1.0 GitHub 发布审计

发布日期：2026-08-25
仓库：https://github.com/qing1989620/workbuddy-modex
分支：main
发布 tag：v0.1.0（annotated，指向 commit 18f6810）
定位：Portable Core Alpha（Pre-release）

## 提交历史

```
18f6810 fix(ci): avoid SIGPIPE from head truncating ommw --help (pipefail)
f7fcd5c fix(ci): sync --extra dev for pytest/ruff; inline abs-path scan without pytest import
e73cf22 chore: pre-release cleanup — ignore .workbuddy, scrub local install paths, clarify scientific-skills as NOT FETCHED, add PROJECT_STATUS
e9e540b feat: OMMW v0.1.0 portable core — schemas, CLI, renderers, adapters, providers, tests, CI, docs
```

无 squash、无 rebase、无 force push。历史线性、可追溯。

## 测试证据

| 门控 | 结果 | 备注 |
|---|---|---|
| pytest（本机） | PASS | 20 passed |
| ommw doctor（本机） | WARN | sympy/pandoc/libreoffice 缺失，属预期降级 |
| ommw smoke-test（本机） | PASS | LaTeX 实际编译出 PDF，parity PASS，负面用例 ALL CAUGHT |
| ommw verify（本机） | PASS | RESEARCH VERIFY: VERIFIED |
| secret-scan（工作树+历史） | PASS | 无真实凭证，无 .env/config.local/.venv 跟踪 |
| abs-path scan（核心） | PASS | 无本机私人路径硬编码 |
| license-audit | PASS | 核心 MIT；MathModelAgent 仅 adapter 无 vendor |
| portability scan | PASS | .gitignore 覆盖敏感项；模板/schema/tests 正常跟踪 |
| GitHub Actions Windows | PASS | lint&unit ✓, integration smoke ✓ |
| GitHub Actions Ubuntu | PASS | lint&unit ✓, integration smoke ✓, secret&abs-path scan ✓ |

## 环境

- 推送环境：Windows 11，Python 3.13.14，git 2.55
- CI 环境：GitHub Actions ubuntu-latest + windows-latest，Python 3.13，uv，TeX Live（Linux apt），Pandoc
- 认证：GitHub classic PAT（repo + workflow scope），仅通过 URL 临时传入 git，不持久化到任何配置文件，push 后立即销毁

## 门控通过情况

| 规范门控 | 状态 |
|---|---|
| LOCAL INSPECTION | PASS |
| SECRET AUDIT（含历史） | PASS |
| LICENSE AUDIT | PASS |
| PORTABILITY AUDIT | PASS |
| RELEASE GATE（本地） | PASS |
| REMOTE INSPECTION | PASS（空仓库确认） |
| SAFE PUSH（无 force） | PASS |
| SHA VERIFICATION | PASS（local == remote == gh API） |
| GITHUB ACTIONS | PASS（Windows + Ubuntu 全绿） |
| POST-PUSH SECRET SCAN | PASS |
| TAG | PASS（v0.1.0 annotated，未覆盖） |
| RELEASE | PASS（Pre-release，中文真实 notes） |

## Verified / Degraded / Not Verified

见仓库 PROJECT_STATUS.md 与 release notes。简要：

### Verified
可移植核心、Research Core Schema、LaTeX 渲染（CI 实跑）、Dual parity、反幻觉门控、WorkBuddy adapter、CLI、CI（Windows+Ubuntu）、许可证隔离。

### Degraded
Word 模式（无 pandoc/LibreOffice）、sympy（本机未装）。

### Not Verified
macOS、uv.lock 冻结复现、scientific-skills 实际拉取、Claude Code/Codex adapter、fresh-machine 真实 clone。

## 已知风险

1. uv.lock 未生成 → 依赖精确复现依赖 pyproject.toml 版本范围 + CI 的 `uv sync --extra dev`。
2. Word 端到端未在本机验证（逻辑 + 结构 QA 已测）。
3. macOS 未覆盖。
4. scientific-skills 未实际拉取，per-skill license 审计待 v0.3。
5. PAT token 由用户提供并通过 URL 临时传入；未写入任何持久配置；建议用户在用完后到 GitHub settings 撤销或缩短有效期。

## 仓库元数据

- owner: qing1989620
- name: workbuddy-modex
- visibility: PUBLIC
- default branch: main
- CI: passing (Windows + Ubuntu)
- license: MIT
- release: v0.1.0 Pre-release
